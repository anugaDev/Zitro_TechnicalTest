export class SlotGameConfiguration
{
    static readonly STOP_TWEEN_DURATION = 0.35;

    static readonly CELL_HEIGHT = 120;

    static readonly SPIN_SPEED = 1200;

    static readonly SYMBOL_COUNT = 6;

    static readonly STRIP_BUFFER = 3;

    static readonly REEL_COUNT = 3;

    static readonly REEL_STAGGER_MS = 2000;

    static readonly MIN_ALL_SPIN_MS = 3000;
}
